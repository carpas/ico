var ICOF, ICOFComponent, React, ReactDOM, TestUtils, createElement;

React = require('react');

createElement = React.createElement;

ReactDOM = require('react-dom');

TestUtils = require('react-addons-test-utils');

jest.dontMock('../../../src/components/icof/icof');

ICOFComponent = require("../../../src/components/icof/icof");

ICOF = void 0;

describe("ICOF Component", (function(_this) {
  return function() {
    beforeEach(function() {
      return ICOF = TestUtils.renderIntoDocument(createElement(ICOFComponent));
    });
    it("should render correctly", function() {});
    return it("should declare the right proptypes", function() {
      return console.log(ICOF.propTypes);
    });
  };
})(this));
