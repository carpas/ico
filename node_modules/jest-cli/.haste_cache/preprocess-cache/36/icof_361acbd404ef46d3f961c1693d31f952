var ICOF, ICOFComponent, React, ReactDOM, TestUtils, arrayOf, createElement, number, propTypes, ref, shape, string;

React = require('react');

createElement = React.createElement;

ref = React.PropTypes, string = ref.string, arrayOf = ref.arrayOf, shape = ref.shape, number = ref.number;

ReactDOM = require('react-dom');

TestUtils = require('react-addons-test-utils');

jest.dontMock('../../../src/components/icof/icof');

ICOFComponent = require("../../../src/components/icof/icof");

ICOF = void 0;

propTypes = ICOFComponent.propTypes;

describe("ICOF Component", (function(_this) {
  return function() {
    beforeEach(function() {
      return ICOF = TestUtils.renderIntoDocument(createElement(ICOFComponent, {
        icofName: "Icof Name",
        children: [
          {
            content: "Teste 2",
            id: 1
          }, {
            content: "Teste 3",
            id: 2
          }, {
            content: "Teste 4",
            id: 3
          }
        ]
      }));
    });
    it("should have a PropType 'icofName' of type: Required String", function() {
      return expect(propTypes.icofName).toBe(string.isRequired);
    });
    return it("should have a PropType 'children' of type: ArrayOf Shape", function() {
      return expect(propTypes.children).toBe(arrayOf(shape({
        content: string,
        id: number.isRequired
      })));
    });
  };
})(this));
