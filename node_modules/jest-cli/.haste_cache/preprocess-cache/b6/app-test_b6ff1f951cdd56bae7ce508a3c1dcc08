var FirstAppChild, React, ReactDOM, TestUtils;

React = require('react');

ReactDOM = require('react-dom');

TestUtils = require('react-addons-test-utils');

FirstAppChild = require('../../src/components/ico-canvas/ico-canvas');

require('../../src/components/app');

describe('App initialization', (function(_this) {
  return function() {
    return it('should initialize App correctly', function() {
      var firstAppChild;
      firstAppChild = ReactDOM.findDOMNode(FirstAppChild);
      return expect(firstAppChild).to.not.equal(void 0);
    });
  };
})(this));
